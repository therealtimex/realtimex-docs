---
title: "Overview"
description: "Embedding models are specific types of models that turn text into vectors, which can be stored and searched in a vector database - which is the foundation of RAG."
---

import { FeatureCard } from "@/components/FeatureCard";
import { Callout } from "nextra/components";

# Embedder Configuration

Embedding models are specific types of models that turn text into vectors, which can be stored and searched in a vector database—the foundation of Retrieval Augmented Generation (RAG).

<Callout type="info" emoji="💡">
  **Tip:** Embedding models are set system-wide and cannot be configured per workspace like LLMs can.
</Callout>

## Supported Embedding Model Providers

<Callout type="warning" emoji="⚠️">
  **HEADS UP!**
  <br/>
  Once you select your embedding model provider and begin uploading and embedding documents it is best to not change it.
  <br/><br/>
  While you *can* change embedders, doing so means you must delete your uploaded documents and re-embed them with the new provider.
</Callout>

RealTimeX supports many embedding model providers out of the box, often with zero configuration.  
You can change your embedding provider and model at any time—but doing so can result in broken queries and will require you to re-embed all uploaded/stored documents.

### Local Embedding Model Providers

<div style={{
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(230px, 1fr))",
  gap: "1rem",
  margin: "1.2rem 0"
}}>
  <FeatureCard title="Built-in (default)" href="local/built-in">
    <img
      src="/images/ai-providers/realtimex.png"
      alt="RealTimeX Built-in (default)"
      height={38}
      style={{
        display: "block",
        marginBottom: 8,
        maxHeight: 38,
        width: "auto",
        maxWidth: 80,
        objectFit: "contain"
      }}
    />
    <div>Private, high-performance embedding engine—no setup needed.</div>
  </FeatureCard>
  <FeatureCard title="Ollama" href="local/ollama">
    <img
      src="/images/ai-providers/ollama.svg"
      alt="Ollama"
      height={38}
      style={{
        display: "block",
        marginBottom: 8,
        maxHeight: 38,
        width: "auto",
        maxWidth: 80,
        objectFit: "contain"
      }}
    />
    <div>Run local, open-source embedding models.</div>
  </FeatureCard>
  <FeatureCard title="LM Studio" href="local/lmstudio">
    <img
      src="/images/ai-providers/lmstudio.svg"
      alt="LM Studio"
      height={38}
      style={{
        display: "block",
        marginBottom: 8,
        maxHeight: 38,
        width: "auto",
        maxWidth: 80,
        objectFit: "contain"
      }}
    />
    <div>Easy desktop app for running embedding models locally.</div>
  </FeatureCard>
  <FeatureCard title="Local AI" href="local/localai">
    <img
      src="/images/ai-providers/localai.png"
      alt="Local AI"
      height={38}
      style={{
        display: "block",
        marginBottom: 8,
        maxHeight: 38,
        width: "auto",
        maxWidth: 80,
        objectFit: "contain"
      }}
    />
    <div>Flexible, open-source backend for local embedding inference.</div>
  </FeatureCard>
</div>

### Cloud Embedding Model Providers

<div style={{
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(230px, 1fr))",
  gap: "1rem",
  margin: "1.2rem 0"
}}>
  <FeatureCard title="OpenAI" href="cloud/openai">
    <img
      src="/images/ai-providers/openai.svg"
      alt="OpenAI"
      height={38}
      style={{
        display: "block",
        marginBottom: 8,
        maxHeight: 38,
        width: "auto",
        maxWidth: 80,
        objectFit: "contain"
      }}
    />
    <div>Industry-standard cloud embeddings (Ada, Babbage, etc).</div>
  </FeatureCard>
  <FeatureCard title="Azure OpenAI" href="cloud/azure-openai">
    <img
      src="/images/ai-providers/azure.svg"
      alt="Azure OpenAI"
      height={38}
      style={{
        display: "block",
        marginBottom: 8,
        maxHeight: 38,
        width: "auto",
        maxWidth: 80,
        objectFit: "contain"
      }}
    />
    <div>Enterprise-grade embedding via Azure’s managed OpenAI service.</div>
  </FeatureCard>
  <FeatureCard title="Cohere" href="cloud/cohere">
    <img
      src="/images/ai-providers/cohere-color.svg"
      alt="Cohere"
      height={38}
      style={{
        display: "block",
        marginBottom: 8,
        maxHeight: 38,
        width: "auto",
        maxWidth: 80,
        objectFit: "contain"
      }}
    />
    <div>Advanced multilingual and domain-specific embeddings.</div>
  </FeatureCard>
</div>

---
title: "Overview"
description: "Choose the default language model provider, connect local or cloud backends, and find provider-specific setup guides."
---

import { FeatureCard } from "@/components/FeatureCard";
import { Callout } from "nextra/components";

# LLM Setup

This section explains how to connect language model providers in RealTimeX.

Use it when you need to:

- choose the default system LLM
- connect a cloud or local provider
- select the default model for that provider
- find the provider-specific setup page for a backend you want to use

Open the main selector from `Settings > AI Providers > LLM`.

<Callout type="info" emoji="💡">
  Use [Large Language Models](/features/language-models) when you want the behavior guide for system defaults, workspace overrides, and provider strategy. This section is for connection and setup.
</Callout>

## What this setting controls

The `LLM` page is the instance-wide source of truth for the default chat model provider.

From that page, you configure:

- the system default provider
- provider credentials, endpoints, or base URLs
- the default model used by that provider
- plugin-provided LLM providers when plugins register them
- the provider list used by workspace and agent override pickers

## Common setup flow

Most providers follow the same setup pattern:

1. Open `Settings > AI Providers > LLM`.
2. Select the provider you want to use.
3. Enter the required connection details.
4. Choose a model, or type a model ID when the provider expects manual entry.
5. Save the configuration.
6. Test the result in a normal chat before using it in workspaces, agents, or automations.

Depending on the provider, RealTimeX may load the model list only after you enter a valid API key, endpoint, or base URL.

## How provider selection layers work

### System default

The system LLM is the fallback provider for the instance.

If nothing more specific is selected, RealTimeX uses this provider for:

- regular chats
- new workspaces that inherit the default
- tools or automations that do not define their own provider

### Workspace override

Workspaces can stay on `System default` or choose a different provider and model for that workspace only.

Use workspace overrides when:

- one team needs a different vendor
- one workspace needs a larger context window
- one project should stay on local inference while the rest of the instance uses cloud models

### Agent and runtime override

Some agent surfaces can override the inherited provider and model as well.

If an agent or runtime does not define its own LLM, it falls back to the workspace or system selection, depending on that feature's configuration.

## Which provider type should you choose?

### RealTimeX-managed options

- `RealTimeX Cloud` is the simplest hosted path when you want a managed cloud provider inside RealTimeX.
- `RealTimeX Local` is the integrated local GGUF workflow when you want on-device inference without running a separate model server yourself.

`RealTimeX Cloud` is configured directly from the same `LLM` selector and normally does not require a separate setup flow beyond choosing the provider and model.

### External cloud providers

Use providers like `OpenAI`, `Anthropic`, `Gemini`, `Azure OpenAI`, `Groq`, or `Mistral` when your team already operates around that vendor or you want access to that model family directly.

### Local or self-hosted endpoints

Use providers like `Ollama`, `LM Studio`, `Local AI`, `KoboldCPP`, `llama.cpp`, or `Generic OpenAI` when you already run local inference infrastructure or a self-hosted gateway.

### Compatibility and proxy layers

Use `Generic OpenAI` or `LiteLLM` when you want RealTimeX to connect through an OpenAI-compatible proxy or compatibility layer instead of a vendor-specific integration.

### Plugin providers

Plugins can register additional LLM providers. When that happens, they appear in the same selector as built-in providers and follow the same general setup flow.

## Local and managed setup guides

<div style={{
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(230px, 1fr))",
  gap: "1rem",
  margin: "1.2rem 0"
}}>
  <FeatureCard title="RealTimeX Local" href="/realtimex-local">
    <div>Managed on-device GGUF runtime, download flow, and local model execution.</div>
  </FeatureCard>
  <FeatureCard title="Local Models" href="/local-models">
    <div>Model inventory, status, pulling, downloading, warming, and deletion across local backends.</div>
  </FeatureCard>
  <FeatureCard title="Ollama" href="/setup/llm-configuration/local/ollama">
    <div>Connect to an Ollama server and choose one of your installed models.</div>
  </FeatureCard>
  <FeatureCard title="LM Studio" href="/setup/llm-configuration/local/lmstudio">
    <div>Use LM Studio as a local LLM backend for RealTimeX.</div>
  </FeatureCard>
  <FeatureCard title="Local AI" href="/setup/llm-configuration/local/localai">
    <div>Connect to a Local AI deployment for self-hosted inference.</div>
  </FeatureCard>
  <FeatureCard title="KoboldCPP" href="/setup/llm-configuration/local/kobaldcpp">
    <div>Use KoboldCPP for GGUF-based local model serving.</div>
  </FeatureCard>
  <FeatureCard title="llama.cpp" href="/setup/llm-configuration/local/llamacpp">
    <div>Connect to a running llama.cpp server and select one of its loaded models.</div>
  </FeatureCard>
  <FeatureCard title="Oobabooga Web UI" href="/setup/llm-configuration/local/oobabooga-web-ui">
    <div>Point RealTimeX at a Text Generation Web UI endpoint served by Oobabooga.</div>
  </FeatureCard>
  <FeatureCard title="Dell Pro AI Studio" href="/setup/llm-configuration/local/dell-pro-ai-studio">
    <div>Use a Dell Pro AI Studio endpoint and choose one of its loaded text models.</div>
  </FeatureCard>
</div>

## Cloud setup guides

<div style={{
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(230px, 1fr))",
  gap: "1rem",
  margin: "1.2rem 0"
}}>
  <FeatureCard title="OpenAI" href="/setup/llm-configuration/cloud/openai">
    <div>Connect a standard OpenAI API key and choose an OpenAI chat model.</div>
  </FeatureCard>
  <FeatureCard title="Azure OpenAI" href="/setup/llm-configuration/cloud/azure-openai">
    <div>Use Azure-hosted OpenAI deployments with endpoint and deployment configuration.</div>
  </FeatureCard>
  <FeatureCard title="Anthropic" href="/setup/llm-configuration/cloud/anthropic">
    <div>Connect Claude models directly through Anthropic.</div>
  </FeatureCard>
  <FeatureCard title="Google Gemini" href="/setup/llm-configuration/cloud/google-gemini">
    <div>Use Gemini models through Google's hosted API.</div>
  </FeatureCard>
  <FeatureCard title="Groq" href="/setup/llm-configuration/cloud/groq">
    <div>Connect Groq-hosted models for low-latency inference.</div>
  </FeatureCard>
  <FeatureCard title="DeepSeek" href="/setup/llm-configuration/cloud/deepseek">
    <div>Use DeepSeek chat and reasoning models through the hosted DeepSeek API.</div>
  </FeatureCard>
  <FeatureCard title="Fireworks AI" href="/setup/llm-configuration/cloud/fireworks-ai">
    <div>Choose from Fireworks-hosted model families grouped by organization.</div>
  </FeatureCard>
  <FeatureCard title="xAI (Grok)" href="/setup/llm-configuration/cloud/xai">
    <div>Use Grok models through xAI with a standard API key and dynamic model discovery.</div>
  </FeatureCard>
  <FeatureCard title="Mistral AI" href="/setup/llm-configuration/cloud/mistral-ai">
    <div>Use Mistral-hosted models through the Mistral API.</div>
  </FeatureCard>
  <FeatureCard title="Cohere" href="/setup/llm-configuration/cloud/cohere">
    <div>Connect Cohere command models through a hosted API key.</div>
  </FeatureCard>
  <FeatureCard title="OpenRouter" href="/setup/llm-configuration/cloud/openrouter">
    <div>Route model access through OpenRouter's multi-provider catalog.</div>
  </FeatureCard>
  <FeatureCard title="Together AI" href="/setup/llm-configuration/cloud/together-ai">
    <div>Use Together-hosted open model APIs inside RealTimeX.</div>
  </FeatureCard>
  <FeatureCard title="Perplexity AI" href="/setup/llm-configuration/cloud/perplexity-ai">
    <div>Connect hosted Perplexity models where that provider fits your workflow.</div>
  </FeatureCard>
  <FeatureCard title="Novita AI" href="/setup/llm-configuration/cloud/novita-ai">
    <div>Use Novita-hosted models and optional stream-timeout tuning.</div>
  </FeatureCard>
  <FeatureCard title="Moonshot AI" href="/setup/llm-configuration/cloud/moonshot-ai">
    <div>Connect Moonshot-hosted models through a standard API key and model selector.</div>
  </FeatureCard>
  <FeatureCard title="PPIO" href="/setup/llm-configuration/cloud/ppio">
    <div>Choose from PPIO-hosted models grouped by organization.</div>
  </FeatureCard>
  <FeatureCard title="APIpie" href="/setup/llm-configuration/cloud/apipie">
    <div>Use the APIpie catalog through a single hosted API endpoint.</div>
  </FeatureCard>
  <FeatureCard title="AWS Bedrock" href="/setup/llm-configuration/cloud/aws-bedrock">
    <div>Use foundation models through your AWS Bedrock account.</div>
  </FeatureCard>
  <FeatureCard title="Hugging Face" href="/setup/llm-configuration/cloud/hugging-face">
    <div>Point RealTimeX at a Hugging Face Inference Endpoint.</div>
  </FeatureCard>
  <FeatureCard title="NVIDIA NIM" href="/nvidia-nims/introduction">
    <div>Use a hosted or managed NVIDIA NIM endpoint from the dedicated NIM workflow.</div>
  </FeatureCard>
</div>

## Compatibility and gateway guides

<div style={{
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(230px, 1fr))",
  gap: "1rem",
  margin: "1.2rem 0"
}}>
  <FeatureCard title="Generic OpenAI" href="/setup/llm-configuration/cloud/openai-generic">
    <div>Connect any OpenAI-compatible service through a custom endpoint and model configuration.</div>
  </FeatureCard>
  <FeatureCard title="LiteLLM" href="/setup/llm-configuration/cloud/litellm">
    <div>Route RealTimeX through a LiteLLM proxy and select models from its OpenAI-compatible gateway.</div>
  </FeatureCard>
</div>

## Other providers

Plugins or future builds may still add more providers than this guide lists explicitly.

When that happens, the setup pattern is usually still the same:

- choose the provider
- enter the required API key or endpoint
- select or enter a model
- save and test

## Related guides

- [Large Language Models](/features/language-models)
- [RealTimeX Local](/realtimex-local)
- [Local Models](/local-models)
- [API Access & Keys](/features/api)
- [Credentials](/credentials)

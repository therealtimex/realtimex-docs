---
title: Built-In Plugins
description: Review the plugins that ship with RealTimeX and understand which ones are enabled by default.
---

# Built-In Plugins

Built-in plugins ship with RealTimeX and appear in `Settings > Plugins` with a `Built-in` badge.

These plugins are part of the product, but they still use the same plugin management flow as installed plugins. That means you can usually enable, disable, configure, or reload them from the Plugins page.

## What is different about built-in plugins

- they ship with RealTimeX
- they cannot be uninstalled from the UI
- some are enabled by default
- some expose settings, while others simply add agent skills or provider integrations

## Built-in plugin catalog

| Plugin | Enabled by default | What it adds | Detailed guide |
| --- | --- | --- | --- |
| `Agent Browser` | Yes | Browser automation skill for agents | [Agent Browser](/plugins/agent-browser) |
| `RealtimeX Moderator` | Yes | SDK-backed control of RealTimeX platform features | No separate end-user page yet |
| `QMD` | Yes | Markdown knowledge and notes search skill | [Notes, Wiki & Memory Plugins](/plugins/note-and-knowledge-plugins) |
| `RealtimeX Personal Notes` | Yes | Agent access to personal notes context | [Notes, Wiki & Memory Plugins](/plugins/note-and-knowledge-plugins) |
| `RealtimeX Workspace Memories` | Yes | Agent access to workspace memory files | [Notes, Wiki & Memory Plugins](/plugins/note-and-knowledge-plugins) |
| `RealtimeX LLM Wiki` | Yes | Agent support for wiki-style knowledge projects | [Notes, Wiki & Memory Plugins](/plugins/note-and-knowledge-plugins) |
| `Runtime Auto-Approve` | No | Auto-approve or deny agent permission requests using policy rules | [Runtime Auto-Approve](/plugins/runtime-auto-approve) |
| `OpenAI Image Generation` | No | OpenAI-based image generation provider | [Image Generation Plugins](/plugins/image-generation-plugins) |
| `Google Gemini Image Generation` | No | Gemini-based image generation provider | [Image Generation Plugins](/plugins/image-generation-plugins) |
| `FAL Flux Image Generation` | No | FAL-based image generation provider | [Image Generation Plugins](/plugins/image-generation-plugins) |
| `MiniMax Image Generation` | No | MiniMax image generation provider | [Image Generation Plugins](/plugins/image-generation-plugins) |

## Detailed guides for the most user-visible built-ins

- Use [Agent Browser](/plugins/agent-browser) when you need browser sessions, authenticated browsing, screenshots, or web automation.
- Use [Notes, Wiki & Memory Plugins](/plugins/note-and-knowledge-plugins) when you want to understand how QMD, Personal Notes, LLM Wiki, and Workspace Memories work together.
- Use [Image Generation Plugins](/plugins/image-generation-plugins) when you need provider setup, ordering, or fallback behavior.
- Use [Runtime Auto-Approve](/plugins/runtime-auto-approve) when you are intentionally changing approval policy behavior.

## Default-enabled built-ins

Most default-enabled built-ins add background capability to agents rather than creating a completely separate page.

### Agent Browser

Enabled by default. Pairs with the desktop [Browser Tool](/features/browser-tool) so agents can work inside reusable browser sessions.

### RealtimeX Moderator

Enabled by default. Gives advanced agent workflows direct access to RealTimeX platform features such as workspaces, threads, activities, tools, and related state.

### QMD, Personal Notes, Workspace Memories, and RealtimeX LLM Wiki

Enabled by default. Together these plugins power note retrieval, structured wiki workflows, and background memory recall across notes and working directories.

## Built-in plugins that usually need intentional setup

### Runtime Auto-Approve

This plugin is disabled by default because it changes how permission approvals are handled for ACP and supervised terminal workflows.

Only enable it when you explicitly want policy-based auto-approval behavior.

### Image generation built-ins

The image generation plugins are disabled by default because they usually require API keys and provider configuration before use.

See [Image Generation Plugins](/plugins/image-generation-plugins) for setup.

## Best practices

- Keep the default-enabled skill plugins on unless you have a specific reason to reduce available agent capabilities.
- Treat `Runtime Auto-Approve` as a security-sensitive plugin, not a convenience toggle.
- Enable only the image generation providers you actually plan to configure and use.
